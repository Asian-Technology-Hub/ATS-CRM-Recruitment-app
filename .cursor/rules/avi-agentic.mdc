---
description: Avinash agentic SOP — point at PROJECT.yaml, AGENTS.md, and avi-agentic-engineering. Always on.
alwaysApply: true
---

# Avinash agentic SOP (Cursor)

Follow the **Avinash Agentic Software Design Pattern Catalog** (`AVI-AGENT-001`–`060`). Do not improvise a one-shot prompt workflow.

**Load, in this order (do not dump the catalog):**

1. `PROJECT.yaml` — control plane
2. `AGENTS.md` — **this project's** policy (keep the Next.js stamp intact)
3. `.agents/skills/avi-agentic-engineering/SKILL.md` — for any non-trivial task

Progressive disclosure: retrieve individual catalog cards from `references/catalog.md` only when needed (`AVI-AGENT-033`, `AVI-AGENT-034`).

Project constraints (tenancy, stack, commands, autonomy) live in `AGENTS.md` / `PROJECT.yaml`, not in this rule (`AVI-AGENT-010`).
