# Project knowledge base (`AVI-AGENT-031`)

This folder is **Vetra-scoped**. Do not dump a global second brain here.

- One topic per file. Use the template in `.agents/skills/avi-agentic-engineering/templates/kb-item.md`.
- Keep source URL/file, date, and confidence (`AVI-AGENT-035`).
- Raw captures stay `status: raw` until reviewed (`AVI-AGENT-036`).
- Agents retrieve on demand (`AVI-AGENT-033`) — do not inject this whole directory into every prompt.
